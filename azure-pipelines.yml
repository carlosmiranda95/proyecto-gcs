trigger:
- master

pool:
  vmImage: ubuntu-latest

container:
  image: centos:8
  
resources:
  containers:
  - container: openjdk8
    image: openjdk:8
  
steps:
- script: ls 
  displayName: 'B'

- script: sbt compile
  displayName: 'C'

- script: sbt test
  displayName: 'D'