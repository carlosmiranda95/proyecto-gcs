trigger:
- master

pool:
  vmImage: ubuntu-latest

container:
  image: centos:8
  
steps:
- script: bash -c "yum -y install java-1.8.0-openjdk-devel"
  displayName: 'B'

- script: stat -c 'sbt compile' /var/run/docker.sock
  displayName: 'C'

- script: stat -c 'sbt test' /var/run/docker.sock
  displayName: 'D'